# JWT Secret - Used for signing authentication tokens **(Optional)**
# Generate it using this command in terminal: 
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_generated_secret_here

# MongoDB Connection String
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/your_database_name?retryWrites=true&w=majority
MONGODB_URI=your_mongodb_connection_string

# NextAuth Secret - Used for encrypting session tokens
# Generate it using this command in terminal:
# openssl rand -base64 32
# OR use the same command as JWT_SECRET above
NEXTAUTH_SECRET=your_generated_secret_here

# NextAuth URL - Your application's URL **(Optional)**
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=

# Added by `npx auth`. Read more: https://cli.authjs.dev
